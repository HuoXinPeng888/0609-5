server:
  port: 8084

spring:
  application:
    name: mq-service
  data:
    redis:
      host: localhost
      port: 6379

# 订单服务地址
order:
  service:
    url: http://localhost:8081

logging:
  level:
    com.shop.mq: DEBUG
    org.springframework.web: INFO
